fastapi==0.115.5
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
SQLAlchemy==2.0.36
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.27.2

# 鉴权
passlib[bcrypt]==1.7.4
# bcrypt >= 4.1 移除了 __about__ 属性，passlib 1.7.4 还在用，必须锁老版本
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# 数据库驱动
psycopg2-binary==2.9.10    # RDS PostgreSQL — 唯一支持的数据库

# 后续接入 MediaPipe / 多模态 AI 时再装：
# mediapipe
# Pillow
# opencv-python-headless
