fastapi==0.115.5
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
SQLAlchemy==2.0.36
python-multipart==0.0.12
python-dotenv==1.0.1

# 数据库驱动（部署到 RDS 时按需启用一个）
# pymysql==1.1.1           # RDS MySQL
# psycopg[binary]==3.2.3   # RDS PostgreSQL
# 本地开发默认使用 SQLite，无需额外驱动。

# 后续接入 MediaPipe / 多模态 AI 时再装：
# mediapipe
# Pillow
# opencv-python-headless
