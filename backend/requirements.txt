fastapi==0.115.5
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
SQLAlchemy==2.0.36
python-multipart==0.0.12
python-dotenv==1.0.1

# 数据库驱动
psycopg2-binary==2.9.10    # RDS PostgreSQL
# pymysql==1.1.1           # RDS MySQL（如果切回 MySQL 再启用）
# 本地若想用 SQLite，把 DATABASE_URL 改回 sqlite:/// 即可。

# 后续接入 MediaPipe / 多模态 AI 时再装：
# mediapipe
# Pillow
# opencv-python-headless
